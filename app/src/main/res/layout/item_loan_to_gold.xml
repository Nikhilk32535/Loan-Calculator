<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="6dp"
    android:foreground="?attr/selectableItemBackground"
    android:outlineSpotShadowColor="@color/gold_yellow">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">
        <!-- Scheme Name -->
        <TextView
            android:id="@+id/tvSchemeName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Scheme Name"
            android:drawableLeft="@drawable/bank"
            android:drawablePadding="10dp"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/text_red" />

            <!-- Scheme Intrest -->
            <TextView
                android:id="@+id/tvSchemeInt"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Scheme Intrest"
                android:textSize="16sp"
                android:gravity="end"
                android:textColor="@color/iconGray" />

        </LinearLayout>
        <!-- Required Gold Amount -->
        <TextView
            android:id="@+id/tvgoldrequired"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="Required Gold: 20 gm"
            android:textSize="15sp"
            android:textColor="@color/textPrimary" />

    </LinearLayout>
</androidx.cardview.widget.CardView>
