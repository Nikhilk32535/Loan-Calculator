<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:state_focused="true" android:drawable="@drawable/round_corner_foucused" />

    <!-- Default state -->
    <item android:drawable="@drawable/round_shape_default" />
</selector>
